FROM openjdk:11
COPY Demo.java .
RUN javac Demo.java
CMD ["java", "Demo"]
# This Dockerfile uses the OpenJDK 11 image as the base image, copies the Java source file into the container, compiles it, and then runs the compiled Java program.
